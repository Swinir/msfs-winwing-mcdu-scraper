mss>=9.0.0
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0
websockets>=12.0
PyYAML>=6.0
pywin32>=306; sys_platform == 'win32'

# Optional — background capture of DirectX windows without pinning on top
windows-capture>=1.5.0; sys_platform == 'win32'

# Optional — improves first-run OCR accuracy (template matching takes over after)
easyocr>=1.7.0

# Optional — AMD/Intel GPU acceleration on Windows (for EasyOCR bootstrap)
# torch-directml is hard to install on many systems and EasyOCR cannot use it
# directly anyway. Uncomment the line below only if you specifically need it:
# torch-directml>=0.2.0
